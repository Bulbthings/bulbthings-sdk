export interface BulbthingsOptions {
    apiToken?: string;
    coreUrl?: string;
    eventsUrl?: string;
    companyId?: string;
    environment?: string;
    geoPosition?: { lat: number; lng: number };
}
